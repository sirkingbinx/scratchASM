# sva (scratch-vm-analyzer)
This is a dynamic scratch-VM code analyzer that can both run and debug your Assembly code.
This used to be scratch-vm-compiler but with my decision to add debugging tools, it made more sense to rename it to the analyzer.